# Changesets

Drop a changeset in this folder when you change a publishable package under `packages/*`.

```
pnpm changeset
```

A bot will pick it up at release time. See [docs/packages/POLICY.md](../docs/packages/POLICY.md) for SemVer rules.
