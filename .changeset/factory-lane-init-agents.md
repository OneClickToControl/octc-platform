---
"@1c2c/cli": minor
---

Factory lane SSOT: `octc init agents`, `@1c2c/cli` product shell (package.json + verify CI), portfolio/OAO pin flags on `init app`, archived duplicate internal templates in favor of npm templates. OAO thin wrapper only on `*-app` template.
