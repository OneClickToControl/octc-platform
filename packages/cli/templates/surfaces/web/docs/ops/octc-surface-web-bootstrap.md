# Bootstrap — `web` surface

Generated by `octc add surface web` / `octc sync surface web`. Complete per [REFERENCE_PRODUCT_MONOREPO](https://github.com/OneClickToControl/octc-platform/blob/main/docs/adoption/REFERENCE_PRODUCT_MONOREPO.md) and `docs/architecture.md`.

**Scope:** **`*-app`** repos only; do not mix **`*-agents`** / **`*-workspace`** deliverables into the app monorepo.

- Update **`active_surfaces`**, **`.octc/monorepo.yaml`**, and **PORTFOLIO** when appropriate.
- Typical convention: `apps/web/**`. If you share types, declare `packages/**` in `paths.web` (and other consumer surfaces).
- Observability: **octc-{product}-web**.
