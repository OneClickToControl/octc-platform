# Bootstrap — `landing` surface

Generated by `octc add surface landing` / `octc sync surface landing`. Complete per [REFERENCE_PRODUCT_MONOREPO](https://github.com/OneClickToControl/octc-platform/blob/main/docs/adoption/REFERENCE_PRODUCT_MONOREPO.md) and `docs/architecture.md`.

**Scope:** this matrix and `octc add|sync surface` apply to **`*-app`** (product monorepo) repos. **`*-agents`** / **`*-workspace`** repos have separate governance; do not mix their paths or jobs into the `*-app` `.octc/monorepo.yaml` without an explicit plan.

- Update **`active_surfaces`**, **`.octc/monorepo.yaml`**, and **PORTFOLIO** (`repo_surfaces`) in the same structural window when appropriate.
- Path convention: `apps/landing/**` and/or `apps/web/**` (see CLI default globs).
- Add CI `paths:` jobs and **octc-{product}-landing** observability when applicable.
