# Ejemplo: metadato de cambio de documentación normativa (sin secretos ni PII).
# Copiar y renombrar; ajustar campos a la convención del repo.

schema_version: 0
id: "example-001"
date: "2026-05-01"
author_type: "human" # human | agent | ci
summary: "Describe en una línea el cambio (p. ej. allowlist L2 bump)."
scope:
  paths:
    - "docs/agents/EXAMPLE.md"
    - "agents/example-acp/manifest.json"
reviewers_required: 1
notes: "Opcional: enlace a issue/ADR interno (sin datos sensibles)."
