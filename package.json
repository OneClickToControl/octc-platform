{
  "name": "octc-platform",
  "version": "0.0.0",
  "private": true,
  "description": "OneClickToControl LLC platform monorepo: shared @1c2c/* packages, agent templates, schemas, governance, observability, supply chain, FinOps, DR/BCP.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "lint": "pnpm -r lint",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "verify": "node scripts/verify-structure.mjs"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9"
  }
}
